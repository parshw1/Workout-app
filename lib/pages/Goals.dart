import 'package:flutter/material.dart';

class Goalspage extends StatelessWidget {
  const Goalspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Goals'));
  }
}